  const n1 = 10, n2 =3;

  let sum = n1 + n2;

  let diff = n1 - n2;

  let prod = n1 * n2;

  let quot = n1 / n2;

  let nodu = n1 % n2; 

  let eqLoose = 5 == '5';

  let eqStrict = 5 === '5';
  let greater = 10 > 7;
  
  let andResult = true && false;
  let orResult = true || false;
  let notResult = !false;

  const first = "hello";
  const last = "world";
  const joined = first + "," + last; 
  const template = `${first}, ${last}`;

  let pecOne = 2 + 3 === 4;
  let precTwo = (2 + 3) * 4; 

  console.log(sum, diff, prod, quot, nodu, eqLoose, eqStrict, greater, andResult, orResult, notResult, joined, template, pecOne, precTwo)

